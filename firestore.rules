rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access.
    // All reads/writes go through the backend via Firebase Admin SDK,
    // which bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
